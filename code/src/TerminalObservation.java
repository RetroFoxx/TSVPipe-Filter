/**
 * @author: retrofoxx
 * TerminalObservation is an enum for the terminal observations I have implemented
 * (MAX, MIN) and I have also included NONE to explicitly represent the Null object.
 *
 */

public enum TerminalObservation {
    MIN, MAX, NONE
}
